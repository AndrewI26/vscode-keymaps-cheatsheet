{
  "name": "keybindings-cheatsheet",
  "displayName": "Keybindings Cheatsheet",
  "publisher": "andrewi44",
  "repository": {
    "type": "git",
    "url": "https://github.com/AndrewI26/vscode-keymaps-cheatsheet"
  },
  "icon": "images/icon.png",
  "version": "1.0.0",
  "homepage": "https://github.com/AndrewI26/vscode-keymaps-cheatsheet#readme",
  "engines": {
    "vscode": "^1.60.0"
  },
  "activationEvents": [
    "onCommand:keybindingsCheatsheet.showCheatsheet"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "keybindingsCheatsheet.showCheatsheet",
        "title": "Show Keymaps"
      }
    ]
  },
  "scripts": {
    "compile": "npm run check-types && node esbuild.js",
    "check-types": "tsc --noEmit",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "vscode:prepublish": "npm run package",
    "package": "npm run check-types && node esbuild.js --production"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/vscode": "^1.60.0",
    "copyfiles": "^2.4.1",
    "esbuild": "^0.27.2",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.7.4",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "jsonc-parser": "^3.3.1",
    "vscode-json-languageservice": "^5.6.3"
  }
}
